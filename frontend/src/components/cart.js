import React, { Component } from "react";
import { useCart } from "react-use-cart";

const Cart = () => {
  return (
    <section>
      <div>
        <h5>Hello</h5>
      </div>
    </section>
  );
};

export default Cart;
